import java.util.*;
public class AdaField
{
    static long maxdiff(SortedMap sm)
{
    int max=-1;long diff;
    while(it.hasNext())
    {
        int first=it.next().getKey();
        int second=it.next().getKey();
        diff=second-first;
    if(max>diff)
    return max;
}
}
    public static void main(String args[])
    {
        SortedMap smx<Integer,Integer>=new HashMap<Integer,Integer>();
        SortedMap smx<Integer,Integer>=new HashMap<Integer,Integer>();
        Scanner in=new Scanner(System.in);        
        int t=in.nextInt();
        while(t--)
        {
        int N=in.nextInt();
        int M=in.nextInt();
        int dec=in.nextInt();
        int point=in.nextInt();
    if(dec==0)
    {
        smx.put(x,0);           
    }    
    else
    {
        smy.put(y,0);
    }

}
System.out.println(caldiff(smx)*caldiff(smy));
}